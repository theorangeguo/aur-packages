PACKAGE_SPEC_VERSION=1

PKGNAME=vibe-kanban-bin
PACKAGE_TEMPLATE=binary-archive
UPSTREAM_TYPE=custom-hook

PKGDESC="AI-powered Kanban board"
URL="https://vibekanban.com"
LICENSES=('Proprietary')
ARCHES=('x86_64')
DEPENDS=()
MAKEDEPENDS=('unzip')
OPTIONS=('!strip')
PROVIDES=('vibe-kanban')
CONFLICTS=('vibe-kanban')

SOURCE_RENAME_X86_64='${pkgname}-${pkgver}-x86_64.zip'
PERSIST_STATE_KEYS=('BINARY_TAG')

BINARY_NAME=vibe-kanban
BINARY_SOURCE_PATH=vibe-kanban
INSTALL_BIN_PATH=/usr/bin/vibe-kanban

LOCAL_FILES=()
DOC_FILES=()
LICENSE_FILES=()

INSTALL_MODE=static
INSTALL_FILE='files/vibe-kanban-bin.install'
SERVICE_MODE=static
SERVICE_SCOPE=user
SERVICE_NAME=vibe-kanban.service
SERVICE_FILE='files/vibe-kanban.service'
